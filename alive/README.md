# Alive

Components that survived human review. Exported from the app and eligible for production use.

Each folder:

```
ComponentName/
├── ComponentName.tsx
├── previews.tsx
├── SPEC.md
└── index.ts
```
