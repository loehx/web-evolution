# Dead

Discarded components. Kept for reference — do not export from the app barrel.

Same folder layout as `alive/`. Check here before building to avoid recreating the same design.

When declined with a reason, the explanation is recorded in **`MEMORY.md`** at the project root (and optionally `DECLINE.md` in this folder).
