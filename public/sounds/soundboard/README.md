# Soundboard audio

Place 16 `.m4a` clips here before previewing with sound:

```bash
bash scripts/copy-soundboard-audio.sh
```

Files expected:

- `stahltwiete-2.m4a` … `stahltwiete-16.m4a`
- `zaehne-putzen.m4a`
